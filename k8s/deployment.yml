apiVersion: apps/v1
kind: Deployment
metadata:
  name: myvizion-web
  labels:
    app: myvizion-web
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myvizion-web
  template:
    metadata:
      labels:
        app: myvizion-web
    spec:
      containers:
      - name: myvizion-web
        image: myvizion/website
        ports:
        - containerPort: 80