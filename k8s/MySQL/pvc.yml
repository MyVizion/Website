apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myvizion-mysql-pvc
  namespace: myvizion-web-ns
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: do-block-storage
